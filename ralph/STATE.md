# Ralph Loop — State

> Machine-readable loop state. Only **ws-planner** (status → in_progress) and
> **ws-qa** (verdicts, completion) edit this file. Humans edit the Human Gates
> section. See `SPRINT_INDEX.md` for the active sprint order and routing.

## How to run the loop (VS Code + Copilot)

1. Start from release `master`, then create/open a new feature worktree for the selected sprint.
2. Read repo-local `caveman` + `cavecrew` skills. Use `/caveman full` always;
   route bounded delegated work through cavecrew.
3. `@ws-planner` using GPT 5.6 SOL High writes `ralph/CURRENT_TASK.md`.
4. `@ws-builder` using GPT 5.6 Terra High implements and commits.
5. `@ws-qa` using GPT 5.6 SOL High grades independently. Any FAIL transfers
   implementation to `@ws-fixer` using GPT 5.6 SOL High. Fixer and QA loop
   until PASS; Terra is not re-invoked after its first failed build attempt.

If exact models are unavailable, disclose limitation. Never claim unavailable
model ran. `STATE.md` and `CURRENT_TASK.md` remain durable loop state; cavecrew
messages/tool results may carry transient coordination.

## Workstream status

### Release 5.0 Play-policy follow-up

| Item | Status | Branch | Evidence |
|---|---|---|---|
| Privacy policy + account deletion | complete | `codex/play-policy-delete-account` | Local/full regression, live Supabase, Netlify Forms, 390px draft, and cavecrew security review PASS. |

| WS | Title | Status | Attempts | Score | Commit | Notes |
|----|-------|--------|----------|-------|--------|-------|
| WS-N | Data model & migrations foundation | complete | 1 | 96 | e0ecbbf | Types + migrations 011-013 + sync.ts mappers (push/pull/delete) + data.ts defaults for maintenance/checklists/trips. Migrations still need owner approval before apply (human gate 4). |
| WS-O | Maintenance engine | complete | 1 | 95 | 17ad130 | Car-scoping fix: getComponentStatus now takes savedSetups, resolves weekend.setupId->Setup.carId, excludes unresolvable weekends for scope:'car'. scope:'rig' unaffected (counts all). lint 3-baseline only, build clean. |
| WS-P | Maintenance UI (Trackers "Service", todo-template style) | complete | 1 | 93 | 47da05b | Service sub-tab in Trackers: Car/Rig grouped rows, status chips (ok/due/overdue), usage bars, Log modal (date/type/notes/cost→accounting), Add Component form, Add Defaults seed. Dashboard Service Due collapsible panel (only due/overdue items). App.tsx state+handlers+cloud-pull wired. lint 3-baseline only, build clean. |
| WS-Q | Checklist engine | complete | 1 | 94 | f482938 | Added materializeStarterTemplate() to bridge STARTER_TEMPLATES (plain strings) into real, ID-bearing, user-owned ChecklistTemplate objects. instantiateTemplate/checklistProgress/STARTER_TEMPLATES untouched. lint 3-baseline only, build clean.
| WS-R | Checklist UI | complete | 1 | 92 | f220947 | ChecklistsSection in RaceWeekendView weekend banner (attach templates, check-off with doneAt/doneBy, delete). ChecklistsTab template manager in Trackers (CRUD items, starter templates, expand-to-edit). App.tsx state+pull+delete-weekend-null wired. lint 3-baseline only, build clean. |
| WS-S | Push infrastructure | complete | 1 | 93 | 93d870d | registerForPush (native+web VAPID, graceful no-op, onConflict-token upsert), unregisterPush pre-signOut token delete, firebase-messaging-sw.js scoped + excluded from Workbox precache, send-push edge fn deployed LIVE (verify_jwt; 401/405/no-user-401 smoke-passed; 400/403/200+prune inspection-only, deferred to WS-Y). OWNER ACTION: add `nimbus.engineering.crewchief://auth-callback` to Supabase Auth → URL Configuration → Redirect URLs or native Google sign-in breaks. |
| WS-T | Location sharing (foreground) | pending | 0 | — | — | |
| WS-U | Ping / come-here notifications | pending | 0 | — | — | Needs WS-S |
| WS-V | HERE truck-routing lib | complete | 1 | 93 | 3694aed | flexpolyline decoder + here.ts (geocode/truckRoute/POI discover/planTrip, full HereError taxonomy); live-API verified, lint/build green |
| WS-W | Trip planner UI | pending | 0 | — | — | Needs WS-V |
| WS-X | Teammates on route | pending | 0 | — | — | Needs WS-T + WS-W |
| WS-Y | QA hardening & release | pending | 0 | — | — | Last; gates the batch |
| WS-Z | July 11 product simplification | in_progress | 1 | — | — | Code complete. Migration 014 applied 2026-07-11 and verified live 2026-07-14 with load-session/lifecycle migrations. Remaining authenticated/offline/theme/zoom/mobile QA is absorbed by UXF-9. |

Status values: `pending` · `in_progress` · `complete` · `scaffolded` (partial pre-work exists)

## Human gates (Maxx) — required before the flagged WS can start

- [x] 1. Firebase project + `google-services.json` in `android/app/` (→ WS-S) — done 2026-07-10; pkg nimbus.engineering.crewchief, gitignored
- [x] 2. `FCM_SERVICE_ACCOUNT_JSON` secret set in Supabase + web VAPID key in `.env.local` (→ WS-S) — done 2026-07-10
- [x] 3. HERE account + `VITE_HERE_API_KEY` in `.env.local` (→ WS-V) — done 2026-07-10
- [x] 4. Apply migrations 011–013 to live Supabase (→ before WS-O/Q/S/T cloud sync testing) — applied 2026-07-10
- [x] 5. Apply migration 014 team-delete policies — applied 2026-07-11; verified live 2026-07-14 with `20260714010630` and `20260714020037`

**All listed migration/configuration gates are cleared.** UXF-9 owns remaining runtime acceptance; no UXF-1…8 schema gate exists.

## Grade log

_(ws-qa appends one line per verdict: date · WS · attempt · PASS/FAIL · score · summary)_
- 2026-07 · WS-N · attempt 1 · PASS · 96 · sync.ts mappers (maintenance_components, maintenance_logs, checklist_templates, weekend_checklists, saved_trips) + data.ts INITIAL_* defaults. lint 3-baseline only, build clean. Diff scoped to sync.ts + data.ts only.
- 2026-07 · WS-O · attempt 1 · PASS · 95 · Fixed car-scoping gap in getComponentStatus (added savedSetups param, resolves weekend->car via setupId indirection, rig scope unaffected). Diff scoped to maintenance.ts only. lint 3-baseline only, build clean. No callers yet (App.tsx wiring deferred to WS-P, by design).
- 2026-07 · WS-Q · attempt 1 · PASS · 94 · Added materializeStarterTemplate() — starter templates were plain string[] items but instantiateTemplate() needs full ChecklistTemplateItem[] w/ ids; new fn bridges the gap. Diff scoped to checklists.ts only. lint 3-baseline only, build clean. No callers yet (App.tsx/UI wiring deferred to WS-R, by design).
- 2026-07 · WS-P · attempt 1 · PASS · 93 · Full maintenance UI: Service sub-tab (Car/Rig sections, usage bars, status chips, Log modal, Add form, Add Defaults), Dashboard Service Due panel. App.tsx state+pull wiring. Accounting auto-entry on cost log. lint 3-baseline only, build clean.
- 2026-07 · WS-R · attempt 1 · PASS · 92 · Weekend checklists in RaceWeekendView (attach from template, check-off w/doneAt stamp, delete, blank option). ChecklistsTab template manager in Trackers (CRUD, starter seeds, item add/delete). App.tsx state+cloud-pull+delete-weekend nulling. lint 3-baseline only, build clean.
- 2026-07 · WS-V · attempt 1 · PASS · 93 · All gates + live HERE API verification (decode test vector, geocode, 5.5mi truck route, 10 sorted truck stops, full error taxonomy) passed; Discover text-search POI trade-off accepted; package.json WS-S deps to re-attribute. Commit 3694aed.
- 2026-07 · WS-S · attempt 1 · PASS · 93 · All hard gates met; push register/unregister + dual-SW build verified live; send-push deployed (verify_jwt) with 401/405/no-user-401 live smoke; 400/403/200/prune + device rendering inspection-verified, deferred to WS-Y. Commit 93d870d.
- 2026-07-11 · WS-Z · attempt 1 · PROVISIONAL PASS · — · Cavecrew diff review and deterministic data checks pass; lint has baseline 3 errors only; build and Netlify draft HTTP smoke pass. Migration 014 later applied and verified; remaining authenticated/offline/theme/zoom/mobile runtime QA is carried by UXF-9.

## Backlog

_(follow-up items discovered during QA — do not expand an in-flight WS)_
- WS-V: re-attribute `firebase` + `@capacitor/push-notifications` package.json additions to WS-S. [RESOLVED 2026-07-10 — committed under WS-S 93d870d.]
- WS-Y: live-verify send-push authenticated paths (400 malformed, 403 non-teammate, 200 success + notifications insert, dead-token prune) with a real user JWT, plus actual push RENDERING on Android APK + web PWA. Inspection-only so far.
- WS-S: `com.racenotes.app` deep-link scheme still kept alongside the nimbus scheme (back-compat) — confirm intentional or remove in a later pass.
- WS-V: consider HERE category-ID filtering as a fallback to Discover text search for non-English locales / noisy results.
- WS-S: package-name rename to `nimbus.engineering.crewchief` is complete; old `preview-v2` pause note is historical and no longer gates current UX work.
- WS-Z: migration 014 complete; UXF-9 absorbs authenticated team-delete, offline reload, theme/font, and mobile presentation closeout.


## UX Overhaul (branch `preview-v3`, started 2026-07-12)

Separate track from WS-N…Z. Chunks 1–9 are historical and archived under `docs/archive/`; current correction work starts at `SPRINT_INDEX.md` and `plan-v3-ux-corrections.md`. GPT 5.6 SOL High plans/QAs; GPT 5.6 Terra High gets one initial build pass; any FAIL transfers fixes to SOL High. Runtime metadata is authoritative and unobservable identity is `unverified`. `/caveman full`.

| Chunk | Scope | Status | Commit |
|---|---|---|---|
| 1 | Shared UI primitives (`components/ui/*`, `lib/undo.ts`) + glove/glare/zoom hardening CSS | ✅ complete (Fable QA PASS-w-notes) | e04b15c |
| 2 | App shell: 6→5 tabs (QuickRef→HelpSheet), race-day order, ContextStrip (car+weekend, car hidden ≤1), sunlight toggle, auto-create-first-car, auto-activate-weekend, scroll preservation + Android back (`backStack.ts`), offline badge (`saveStatus.ts`) | ✅ complete (PASS, no blockers) | 306445d |
| 3 | Sessions owns weekends (modals moved out of App.tsx, −451 lines), quick-log top-to-bottom + SAVE RUN, `LapTimeKeypad`, 3×3 diagnostics `SegmentedGrid`, `sessionSequence.ts` auto-name/prefill | ✅ complete (PASS, no blockers; pure fns harness-PASS) | c68f27d |
| 4 | Dashboard: launchpad + "+LOG RUN" hero + first-run card + sorting + ⋯/Undo + scoped quick-service/Accounting | ✅ complete | 21405e9 |
| R1 | **URGENT pre-Chunk-5 regression repair:** Dashboard-first team banner, WCAG light accent/type floor, named AFCO-scoped Tuning Guide, deterministic exact starter-template convergence | ✅ complete — CODE_PASS + RUNTIME_PASS; Android 4.0 (15); SOL→Terra→SOL metadata verified | b6d702e |
| 5 | Setups: corner cards + steppers, **four-bar quick-adjust (fast, not hidden)**, tires sub-view, copy-last-setup, diff entry, propagation toast | ✅ complete — CODE_PASS + RUNTIME_PASS | d5ef1f4 |
| 6A | Setup/measurement refinement: blank defaults, complete LR/RR top+bottom four-bar rows, tire summary, Load Session naming/ride height, two font choices, plain priority help | ✅ complete — SOL QA PASS; CODE_PASS + local RUNTIME_PASS + Netlify draft + live migration verified | ae9395b |
| 6B | Setup history lifecycle: immutable baseline, editable Weekend Setup, always-available Finish Weekend, immutable final snapshot, editable Current Setup copy | ✅ complete — SOL QA attempt 3 PASS | 2a941d3 + 874fecc + 7b58b30 |
| 7 | Quick Adjust expansion: spring rate/rounds, ride-height review flag, shock note/load graph, J-bar, gear, active-setup change log | ✅ complete — SOL QA attempt 2 PASS | 58e4522 + 030122c |
| 8 | Trackers: hybrid Main Checklist, merged Templates, Maintenance Logs, 90%-interval task injection, after-create assignment, accounting defaults | ✅ complete — SOL QA PASS | 2f60420 |
| 9 | Export share, contextual help, racing-language copy audit, final two-size/font/glove regression | ✅ complete — SOL QA attempt 2 PASS | c8c4a21 + 254d928 |

## UXF correction batch (Sprint 1, started 2026-07-14)

| UXF | Scope | Status | Attempts | Score | Commit | Notes |
|---|---|---|---|---|---|---|
| UXF-1 | Duplicate Other + shared active-checklist projection | complete | 0 | 97 | 94d1e79 | SOL QA PASS: single Other, shared pure active projection across ToDo/Dashboard, persistence-safe harness, exact lint baseline, build, diff, and cavecrew gates clean. |
| UXF-2 | Quick Adjust net-outcome coalescing | complete | 0 | 96 | 1dd5b75 + f03a17b | SOL QA attempt 2 PASS: run-scoped net rows plus safe partial-history failure; full harness, persistence, locks, exact lint baseline, build, diff, and cavecrew gates clean. |
| UXF-3 | Starting / Live-Trackside / Raced-or-Finished setup labels | complete | 0 | 97 | 72ea4f8 | SOL QA attempt 1 PASS: conditional Raced/Finished labels, legacy display-only mapping, storage/cloud purity, lifecycle regressions, harness, exact lint baseline, build, diff, clean tree, and cavecrew review pass. |
| UXF-4 | Load graph travel axis + measured-height display | complete | 0 | 96 | 0c2c827 + 12d932f | SOL QA attempt 2 PASS: upward travel plots, common comparison zero, raw-height SVG/PNG detail, usable point hit/focus controls, unchanged data paths, harness, exact lint baseline, build, diff, clean tree, and cavecrew review pass. |
| UXF-5 | Copy tone + Tuning Guide/App Guide separation | complete | 0 | 96 | e2f0553 + 83e652f | SOL technical QA attempt 2 PASS; Maxx approved tone on draft `6a5682d667a1ecb5c117bf39`. Guide routing, concise maintenance display, raw-data preservation, full 134-effect translation, harnesses, exact lint baseline, 555-module/18-entry build, diff, clean tree, and cavecrew review pass. UXF-6 unlocked. |
| UXF-6 | Races/Days maintenance intervals + starting usage | complete | 0 | 96 | 6cae6cf + 9d0fb26 | SOL QA attempt 2 PASS after calendar repair: distinct Feature weekends, ISO/local date boundaries, DST-safe Days, local service defaults, starting usage/reset, mapper, migration, 90% behavior, harness, exact lint baseline, 556-module build, diff, clean tree, cavecrew, live schema/RLS, and mobile draft shell pass. |
| UXF-7 | Main Checklist redesign proposal | complete | 0 | 100 | b1855a6 | Evidence-backed proposal with 74 valid file:line references and clean cavecrew review. Maxx approved it as written on 2026-07-14; no application code changed in UXF-7. |
| UXF-8 | Docs and loop-state hygiene | complete | 0 | 96 | 90138be | SOL QA attempt 3 PASS: all active routing, cold-start authority, links, archives, migration state, UXF table, scope, and diff gates clean. UXF-1 unlocked. |
| UXF-10 | Approved Main Checklist redesign build | complete | 2 | 96 | 3b40a1e + 075be90 | SOL QA attempt 2 PASS after narrow SOL repair: current visible recurrence wins over hidden duplicates; nested keyboard controls cannot complete rows. Lifecycle/UI, JSONB mapper, focused harness, exact lint baseline, 557-module build, diff, clean tree, cavecrew review, prior Android runtime, and 320/390 draft shell checks pass. No SQL or second checklist system. |
| UXF-9P | Owner preflight: no-car guidance, Race Day copy, Tuning Guide regression | complete | 0 | 97 | 0361278 | Direct Garage routing and hard creation guards, shared prompts, finder-first regression, display-only Race Day vocabulary, full harness/build/lint/diff gates, cavecrew PASS, and final draft clean at 320/390 signed-out. |
| UXF-9 | Batch regression, draft, and owner acceptance | complete | 0 | 95 | Release 5.0 | Owner authorized master/GitHub/Android release. Ten harnesses, exact lint baseline, web build, signed APK/AAB, package/version, and certificate gates pass. Separate Claude Fable authenticated plan was not run and is disclosed. |

## UX Overhaul v2 (branch `codex/ux-overhaul`, started 2026-07-17)

Execution authority: `docs/UX_TECHNICAL_REVIEW_2026-07-17.md` Part 5. Tasks run serially with Terra initial builds and SOL light QA. Runtime model identity comes only from turn metadata; when unavailable it is recorded as `unverified`.

| Task | Scope | Status | Attempts | Score | Commit | Notes |
|---|---|---|---|---|---|---|
| A1 | Scale default and migration | complete | 1 | 97 | dc49c06 | SOL light QA PASS. `chunk6a-refinement-harness.ts` PASS; lint matched exact three-error baseline; 567-module build PASS; diff contained exactly four authorized files, `git diff --check` passed, and worktree was clean; cavecrew review found no issues. Runtime model unverified. |
| A2 | Save Run reservation and bottom-nav clearance | complete | 1 | 96 | 141b17d | SOL light QA PASS. Focused chunk5 harness PASS twice; in-memory reservation and dirty-only mutation proofs PASS; lint matched exact three-error baseline; 567-module build PASS; exact four-file diff, `git diff --check`, and clean-tree gates PASS; A1 scale, Quick Adjust, and offline regressions PASS; cavecrew found no issues. Three unrelated stale byte-lock harness failures were evidence only, not A2 defects: `muted-text-color` AuthView lock, `saved-flash` whole-App reconstruction, and `setup-touch-target` ConfirmSheet lock. Signed-in viewport visual QA remained unauthorized; no credentials created or used. Runtime model unverified. A3 unlocked. |
| A3 | Tire-pressure grid and stepper layout | complete | 1 | 96 | 4618ce3 | SOL light QA PASS. Focused setup-touch-target harness and in-memory grid/target/timer/touch mutation proof PASS; tire, A2 setup/reservation, A1 scale, Quick Adjust, and offline regressions PASS; lint matched exact three-error baseline; 567-module build PASS; exact three-file diff, `git diff --check`, and clean-tree gates PASS; cavecrew found no issues. Auth-gated shell had zero horizontal overflow at 360×800, 390×844, 412×915, and 1080×2118; signed-in pressure-grid visual QA remained unauthorized, and no credentials were created or used. Runtime model unverified. A4 unlocked. |
| A4 | App-wide density and whitespace pass | complete | 4 | 98 | d821ad6 + 2f668ce + 25130cd + 2fcebc4 | Independent final Chunk A QA PASS. Exact Weather and Best/Finish production-span mutations now fail both unique source locks and rendered 14px value floors; all A1–A4 acceptance, geometry, safe-area, pinch, focus, boundary, build, and regression gates pass. Only the two documented stale byte locks remain nonblocking. Runtime verified `gpt-5.6-sol` High. |
| B1 | Commit-on-pointerup NumberStepper | complete | 2 | 98 | d87275d + 72c34ac | Final independent SOL QA PASS after harness-only repair made movement and pointercancel mutations behaviorally real. Product, B1 proof, A1–A4, baseline, build, scope, shell, and cavecrew gates pass. |
| B2 | Notification arbiter, unified copy, top toast | complete | 3 | 98 | ba7e018 + 131705c + 546ffa5 | Final independent SOL QA PASS. Simultaneous production routing renders exactly one info notification; the compile-real `isInfo` priority mutation renders Saved, hides info, and independently fails source, model, and rendered gates. All B2 timing/copy/dedupe/geometry/co-render, four modernized regressions, B1/A1–A4, baseline, build, shell, scope, and cavecrew gates pass. Only the two unchanged documented stale byte locks remain nonblocking. B3 unlocked for one Terra High initial build. |
| B3 | Honest typed save/sync/delete feedback | in_progress | 3 | 90 | 95c0ceb + 5e660d9 + efe534c | Independent final SOL QA FAIL. Owner-approved repair fixed prior selection, pull-generation, terminal-entry, and shared-caller wiring blockers, but two races remain: later `flashSaved()` can arm while terminal status persists and reappear after acknowledgement; Export shared-pull failure callback lacks cleanup/current-user protection. SOL owns narrow repair; Terra permanently out; B4 blocked. Runtime model unverified. |

### UX Overhaul v2 grade log
- 2026-07-18 · B3 · attempt 3 · FAIL · 90 · Owner-approved SOL repair commit `efe534c8`; clean exact cumulative B3 scope, selection-only activation/pressure propagation without false persistence, main pull query/catch reporting and generation guard, terminal precedence, shared-pull failure routing, payload/merge/throttle/queue/delete/zero-row preservation, focused regressions, exact 22/24 harness matrix, exact three-error lint baseline, 566-module build, four-viewport signed-out shell, diff/protected checks, and cavecrew review completed. Two blocking races remain: while `sync-error` or `deferred-delete-retrying` persists, a later `flashSaved()` can arm hidden Saved state that explicit acknowledgement reveals; Export passes its async shared-pull status callback without effect cleanup/current-user guard, so unmounted or superseded-user work can publish terminal error. Proof covers Saved-before-failure and callback presence, but misses failure-then-later-Saved-then-acknowledge and stale shared-pull completion. SOL repairs only `src/App.tsx`, `src/components/ExportView.tsx`, `scripts/saved-flash-harness.ts`, and assertion-only `scripts/offline-indicator-harness.ts`; other approved B3 files stay unchanged unless essential. B4 blocked. Runtime model unverified.
- 2026-07-18 · B3 · OWNER SCOPE APPROVAL · Maxx approved adding exactly `src/components/SettingsView.tsx` and `src/components/ExportView.tsx` solely to forward App's typed sync-status failure callback through Settings into the actual `pullSharedData` caller. Complete SOL repair scope is `src/App.tsx`, `src/lib/sync.ts`, callback-only `src/components/SettingsView.tsx`, callback-only `src/components/ExportView.tsx`, `scripts/saved-flash-harness.ts`, and assertion-only `scripts/offline-indicator-harness.ts`; `src/lib/saveStatus.ts` remains deleted. No UI/export behavior, copy, payload, merge, throttle, queue, delete, zero-row, schema, native, release, or later-task scope expands. B4 remains blocked. Runtime model unverified.
- 2026-07-17 · B3 · attempt 2 · FAIL · 82 · SOL repair commit `5e660d9`; exact four-file repair scope, cumulative approved B3 deletion of `src/lib/saveStatus.ts`, clean HEAD/protected paths, active-selection and pressure propagation repair, terminal arbiter, per-pull error callback additions, focused B1/B2/B3 markers, offline/resume/touch regressions, exact 22/24 matrix with only two named stale locks, exact three-error lint baseline, 566-module build, four-viewport signed-out shell, diff checks, and cavecrew review completed. Three blockers remain: `reportPullFailure` can publish `sync-error` from a stale pull generation before `isCurrentPull()`; terminal failure does not clear/cancel pending `savedFlash`, so early acknowledgement can resurrect old Saved; `pullSharedData` accepts a callback but its sole production caller in `ExportView` omits it, leaving shared pull query errors/catch UI-silent. B4 blocked. SOL repair awaits explicit owner approval to add exactly `src/components/SettingsView.tsx` and `src/components/ExportView.tsx`, solely to thread App's typed failure callback through Settings into the actual shared-pull caller. Existing repair scope stays `src/App.tsx`, `src/lib/sync.ts`, `scripts/saved-flash-harness.ts`, and assertion-only `scripts/offline-indicator-harness.ts`; `src/lib/saveStatus.ts` stays deleted. Required proof must use compile-real mutations for stale-generation rejection, terminal-entry Saved cancellation plus acknowledgement, and actual caller callback routing. Runtime model unverified.
- 2026-07-17 · B3 · attempt 1 · FAIL · 79 · Terra commit `95c0ceb`; exact approved five-file scope including deleted `src/lib/saveStatus.ts`, clean tree, protected paths, focused B1/B2/B3 markers, offline/pull/touch regressions, 22/24 matrix with only named stale locks, exact three-error lint baseline, 566-module build, four-viewport signed-out shell, and diff checks pass. Blocking product defects: `handleSaveSetups` treats unchanged setup bytes as a blocked no-op even when `activeId` changes, breaking Use Setup and pressure propagation; Supabase pull `{ error }` results/catches do not report `sync-error`; raw automatic setters allow later `synced`, `offline-saved`, `syncing`, or pull completion to overwrite persistent terminal errors before explicit acknowledgement. Proof misses unchanged-array activation, every pull-error branch, terminal-error overwrite sequences, and complete four-state rendered behavior. SOL repair owns only `src/App.tsx`, `src/lib/sync.ts`, `scripts/saved-flash-harness.ts`, and assertion-only `scripts/offline-indicator-harness.ts`; `src/lib/saveStatus.ts` stays deleted. B4 blocked. Runtime model unverified.
- 2026-07-17 · B3 · OWNER SCOPE APPROVAL · Pre-build audit proved required deletion of `src/lib/saveStatus.ts` would otherwise break `scripts/offline-indicator-harness.ts`, which imports and byte-locks that module. Owner authorized only that harness to follow the relocated online-status hook and typed status renderer. No application behavior, product file, sync semantics, stale-lock allowance, or later-task scope expands. Permitted B3 files are now `src/App.tsx`, `src/lib/sync.ts`, deleted `src/lib/saveStatus.ts`, `scripts/saved-flash-harness.ts`, and assertion-only `scripts/offline-indicator-harness.ts`. B3 unblocked for exactly one Terra High initial build; this SOL authority turn runtime model unverified.
- 2026-07-17 · B2 · attempt 3 · PASS · 98 · Final HEAD `546ffa5`; exact harness-only repair range after accepted product commit `131705c` and clean protected paths. The harness extracts production notification routing from `isInfo` through `isSuccess`: simultaneous info + Saved baseline renders exactly one info notification; the real production-source priority mutation compiles, changes `isInfo` routing, renders exactly one Saved while hiding info, and independently fails source, model, and rendered gates. Shared 1500ms Saved/Synced/pull lifetime, single reason-keyed copy with resolved-context dedupe, success clearing before duplicate return, info priority/no co-render, passive historical banner, compiled-CSS wrapped-header/toast/nav containment across four viewports × Standard/Large × online/offline, direct 44px close, and all four modernized regression harnesses pass. B1 and A1–A4 regressions pass. Full matrix is 22/24 with only the two unchanged named nonblocking locks: `muted-text-color-harness.ts` AuthView byte lock and `saved-flash-harness.ts` post-B1/B2 whole-App reconstruction byte lock. Lint matches the exact three-error baseline (`RaceWeekendView` unknown-to-`File`, `SetupView` `key`/`CornerFormProps`, `SmasherLoadsView` unknown-to-`File`); 567-module build passes. Signed-out 360×800, 390×844, 412×915, and 1080×2118 shell checks have exact dimensions, controls at least 44px, no horizontal overflow, pinch zoom enabled, and zero console errors. Diff, protected-path, clean-tree, and cavecrew review pass. Runtime verified `gpt-5.6-sol` High. B2 closed; B3 unlocked for exactly one Terra High initial build.
- 2026-07-17 · B2 · attempt 2 · FAIL · 95 · SOL repair commit `131705c`; exact six-file repair and clean protected paths, shared 1500ms Saved/ordinary Synced/pull Synced lifetime, central resolved-context copy dedupe, success clearing before duplicate return, one arbiter, passive historical banner, real compiled-CSS header/toast/nav geometry across four viewports × Standard/Large × online/offline, direct 44px close, compile-real co-render proof, focused B1/B2 PASS markers, 22/24 full matrix with only the two named stale locks, exact three-error lint baseline, 567-module build, and signed-out four-viewport shell all pass. Blocking proof defect: `infoPriorityMutation` changes and compiles production source and fails source/model gates, but rendered fixture hardcodes info and never derives simultaneous info + Saved priority from mutated `isInfo`; mutation neither renders Saved/hides info nor independently fails rendered gate. SOL attempt 3 is harness-only in `scripts/saved-flash-harness.ts`; product accepted and frozen; B3 blocked. Runtime verified `gpt-5.6-sol` High.
- 2026-07-17 · B2 · attempt 1 · FAIL · 84 · Terra commit `ba7e018`; exact original three-file scope, clean tree, B1/B2 focused PASS markers, exact three-error lint baseline, 567-module build, diff/protected-path checks, and signed-out four-viewport shell pass. Full matrix is 18/24: the two documented stale locks plus four demonstrably obsolete B2 expectations in `car-delete-undo`, `chunk5-setup`, `confirm-sheet`, and `offline-indicator`; those four are not product regressions but cannot be waived or edited under the original work order. Blocking product/proof defects: `Synced` remains visible 2500ms and pull-path `Synced` 3000ms instead of the shared ~1500ms lifetime; `showInfo(reason, message)` still accepts caller-composed copy; top offset is not proven against wrapped Large/offline header; rendered harness hardcodes synthetic geometry and infers close size from the container; co-render/priority mutations do not compile-real exercise production routing. SOL repair scope is `src/App.tsx`, `scripts/saved-flash-harness.ts`, and assertion-only modernization in the four named regression harnesses. `src/components/SetupView.tsx` product is accepted and frozen. Runtime verified `gpt-5.6-sol` High. B3 blocked.
- 2026-07-17 · B1 · attempt 2 · PASS · 98 · Final HEAD `72c34ac`; original product commit `d87275d`, Ralph failure record `d0f318d`, and one-file harness repair have exact permitted ranges and clean protected paths. Pointerdown, scroll movement, pointercancel, and no-move out-of-slop release produce zero writes; in-slop pointerup produces one; `touch-pan-y`, 350ms/100ms repeat, no release double-step, keyboard/editing/ARIA/focus/44px/layout all pass. Repair neutralizes the exact production movement-cancellation block and proves a repeat write at 350ms, routes real pointercancel handlers to `finishPress` and proves a write, and both source gates reject. B1 PASS marker precedes unchanged stale whole-App lock; focused B1/A1–A4/tire/Quick Adjust/offline regressions PASS; full matrix 22/24 with only unchanged documented `muted-text-color` AuthView and `saved-flash` whole-App byte locks; exact three-error lint baseline; 567-module build; signed-out 390×844 shell has 44px controls, no overflow/errors, and pinch zoom; cavecrew PASS; worktree clean. Runtime verified `gpt-5.6-sol` High. B1 closed; B2 unlocked for one Terra initial build.
- 2026-07-17 · B1 · attempt 1 · FAIL · 94 · Terra commit `d87275d`; product state-machine passes pointerdown zero-write, in-slop pointerup one write, no-move out-of-slop release zero, movement/pointercancel zero, `touch-pan-y`, 350ms/100ms repeat with no release double-step, keyboard/editing/ARIA/44px/layout preservation, focused proof, A1–A4 regressions, exact three-error lint baseline, 567-module build, exact three-file/protected-path/clean-tree gates, signed-out shell, and cavecrew. Full matrix is 22/24 with only documented stale `muted-text-color` and `saved-flash` byte locks. Blocking proof defect: `slop-cancel-removed` deletes only `press.moved = true` while `cancelPress()` still cancels, and `pointercancel-write` substitutes zero-write `stopPress`; neither seed proves its advertised behavior regression. SOL repairs only `scripts/saved-flash-harness.ts`; product and setup harness frozen; B2 blocked. Runtime verified `gpt-5.6-sol` High.
- 2026-07-17 · A4 · attempt 4 · PASS · 98 · Harness repair `2fcebc4` uniquely extracts exact Weather `{value}` and Best/Finish production spans, feeds their production-derived class/content into the rendered fixture, and rejects targeted in-memory `text-sm` to `text-xs` production mutations through both the original source lock and rendered 14px value gate. Focused A1–A4, tire, Quick Adjust, and offline regressions PASS; FourBar 12-case and repair eight-case geometry/overflow/target proofs PASS; exact three-error lint baseline and 567-module build PASS; repair diff is harness-only, protected paths and diff-check pass, worktree clean, and cavecrew found no issues. Runtime verified `gpt-5.6-sol` High.
- 2026-07-17 · A1 · attempt 1 · PASS · 97 · Commit `dc49c06`; focused chunk6a harness PASS; exact three-error lint baseline; 567-module build PASS; exact four-file scoped diff, diff-check, and clean-tree gates PASS; cavecrew found no issues; runtime model unverified. A2 unlocked for Terra initial build.
- 2026-07-17 · A2 · attempt 1 · PASS · 96 · Commit `141b17d`; focused chunk5 harness PASS twice; in-memory reservation and dirty-only mutation proofs PASS; exact three-error lint baseline; 567-module build PASS; exact four-file scoped diff, diff-check, and clean-tree gates PASS; A1 scale, Quick Adjust, and offline regressions PASS; cavecrew found no issues; runtime model unverified. Three unrelated stale byte-lock failures (`muted-text-color` AuthView lock, `saved-flash` whole-App reconstruction, `setup-touch-target` ConfirmSheet lock) were evidence only, not A2 defects. Signed-in viewport visual QA remained unauthorized; no credentials created or used. A3 unlocked for Terra initial build.
- 2026-07-17 · A3 · attempt 1 · PASS · 96 · Commit `4618ce3`; focused setup-touch-target harness PASS; in-memory forced-grid, 40px-target, timer, and touch-action mutations were rejected; tire, A2 setup/reservation, A1 scale, Quick Adjust, and offline regressions PASS; exact three-error lint baseline; 567-module build PASS; exact three-file scoped diff, diff-check, and clean-tree gates PASS; cavecrew found no issues; runtime model unverified. Auth-gated shell had zero horizontal overflow at 360×800, 390×844, 412×915, and 1080×2118. Signed-in pressure-grid visual QA remained unauthorized; no credentials created or used. A4 unlocked for Terra initial build.
- 2026-07-17 · A4 · attempt 1 · FAIL · 80 · Commit `d821ad6`; exact 13-file scope, focused A4 harnesses and six mutation checks, A1–A3/tire/Quick Adjust/offline regressions, exact three-error lint baseline, 567-module build, diff/clean-tree gates, and 360×800/390×844/412×915/1080×2118 signed-out shell checks passed. Full matrix was 22 PASS / 2 unrelated stale byte-lock failures (`muted-text-color` AuthView lock and `saved-flash` whole-App reconstruction). Cavecrew and SOL audit found FourBar hole groups receive about 123px at 360px versus at least 134px needed for three 44px targets plus borders, so `overflow-hidden` clips the editable center and Large worsens it; harnesses did not measure width/clipping; named RaceWeekend/Trackers/Settings density tiers remained; diagnostic racing-data input stayed `text-xs`. SOL fixer owns attempt 2; Terra not re-invoked. Signed-in visual QA remains unauthorized; no credentials created or used. Runtime model unverified.
- 2026-07-17 · A4 · attempt 2 · PASS · 97 · SOL repair commit `2f668ce`; rendered Chromium proof passed 12 viewport/scale cases with target rectangles, containment, overlap, and grid/group/center `scrollWidth <= clientWidth`; old field-border, forced-Large-columns, and 43px-target geometry mutations failed, and independent mutation audit rejected 8/8 required assertion categories. Owner order remains Bar Length full-width with Frame/Birdcage below, two columns at Standard and adaptive stacking only under width pressure. Focused A4, A1–A3, tire, Quick Adjust, and offline regressions PASS; full matrix 22/24 with only unrelated stale `muted-text-color` AuthView and `saved-flash` whole-App byte locks; exact three-error lint baseline; 567-module build; original 13-file and repair 6-file scope, compiled adaptive CSS, diff-check, clean tree, and cavecrew review PASS. Signed-out shell at 360×800, 390×844, 412×915, and 1080×2118 had exact dimensions, zoom 1, no horizontal overflow, pinch zoom enabled, and zero console errors. Signed-in feature visual QA remained unauthorized; no credentials created or used. Runtime verified `gpt-5.6-sol` High. Chunk A QA gate unlocked; B1 remains blocked until gate PASS.

### UX chunk grade log
- 2026-07-17 · Chunk A QA · attempt 3 · PASS · 98 · Final HEAD `2fcebc4`; A1–A4 scale/density, dirty-only Save Run, pressure/FourBar geometry, 44px target and text floors, safe-area/nav reservation, focus, offline, pinch-zoom, and frozen pre-B1 NumberStepper behavior pass together. Exact Weather and Best/Finish spans each occur once; independent targeted production mutations remove the original lock and render 12px against the required 14px floor, so both source and Chromium gates reject them. Focused regressions PASS; full matrix 22/24 with only unchanged documented `muted-text-color` AuthView and `saved-flash` whole-App byte locks; exact three-error lint baseline; 567-module build; cumulative/repair diff-check, protected paths, clean tree, and cavecrew PASS. Signed-out 360×800, 390×844, 412×915, and 1080×2118 shell checks show exact dimensions, 44px controls, no horizontal overflow, no sub-12px text, zoom 1, pinch enabled, focus visible, and zero console errors. Signed-in visual interaction remains unauthorized without credentials; production-bound rendered fixtures cover those states. Owner on-device density review recommended at this chunk boundary. Chunk A closed; B1 unlocked for one Terra High initial build.
- 2026-07-17 · Chunk A QA · attempt 2 · FAIL · 92 · Repair `25130cd` product floors pass: Auth/Settings/Race Day targets and semantic body/racing values meet acceptance; signed-out 360×800, 390×844, 412×915, and 1080×2118 shell checks show 44px targets, text floors, focus, pinch zoom, and no overflow; A1–A4/tire/Quick Adjust/offline regressions, exact three-error lint baseline, 567-module build, scoped diff, protected paths, and 22/24 full matrix pass with only unchanged documented stale `muted-text-color` and `saved-flash` byte locks. QA found one blocking proof defect in `setup-touch-target-harness.ts`: Weather and Best/Finish locks match duplicated generic classes, so targeted exact production `text-sm` to `text-xs` mutations leave both source predicates green while only a copied synthetic fixture is mutated. Product components require no repair. Next work is harness-only exact-span extraction plus production-source mutation rejection. B1 remains blocked. Runtime verified `gpt-5.6-sol` High.
- 2026-07-17 · Chunk A QA · FAIL · 82 · Cumulative A1–A4 scope, protected paths, clean tree, A1–A3 semantics, dirty-only Save Run, 350ms/100ms frozen NumberStepper behavior, FourBar 12-case rendered geometry, 11/11 independent mutations, exact three-error lint baseline, 567-module build, and 22/24 full matrix passed; only documented stale `muted-text-color` and `saved-flash` byte locks failed. Signed-out shell at 360×800, 390×844, 412×915, and 1080×2118 had no overflow and retained pinch zoom, but rendered Auth controls measured 32–41.3px; source/cavecrew audit found Settings Reset/Danger and Race Day GPS/Zip/clear below 44px plus Auth/Settings body and Weather/Best/Finish values below `text-sm`. Signed-in every-view/live-tier/owner-scenario visual QA remained unauthorized; no credentials created or used. Failure attributed to A4; bounded SOL repair expands A4 only to `AuthView.tsx`, `SettingsView.tsx`, `RaceWeekendView.tsx`, and necessary A4 harness coverage. B1 remains blocked. Runtime verified `gpt-5.6-sol` High.
- 2026-07-14 · UXF-9 / Release 5.0 · OWNER RELEASE PASS · 95 · Maxx explicitly authorized master promotion, GitHub push, APK, and Play Store AAB. All ten harnesses pass; lint remains exact three-error baseline; 559-module/18-entry web build passes; signed APK and AAB build with package `nimbus.engineering.crewchief`, version code 17/name 5.0, API 36, and matching certificate. Android project files are now reproducible from Git without tracked credentials. Separate Claude Fable authenticated final plan was not executed; emulator install was not forced because existing debug signature differs and preserving device data won.
- 2026-07-14 · UXF-9P · Owner preflight corrections · PASS · 97 · Feature `0361278`; no-car actions route directly to Garage before forms/mutations/follow-on runs, including stale active-event session paths. Shared Setups/Sessions/Dashboard/context guidance, finder-first global Tuning Guide regression, and display-only Race Day terminology landed without internal/schema renames or user-text rewriting. Ten focused/accumulated harnesses PASS; exact three-error lint baseline; 559-module/18-entry build; diff and cavecrew review clean. Final draft `6a56d1d228995bd1cef8c421` has no 320/390 signed-out overflow or console warnings. Authenticated draft feature state unavailable and not claimed. UXF-9 final Claude plan ready.
- 2026-07-14 · UXF-9P · Owner preflight planning · READY · No-car creation must route visibly to Add a Car; visible Weekend terminology becomes Race Day without internal/schema renames; global Tuning Guide finder-only behavior is already current and receives stronger regression coverage. UXF-9 held until correction PASS and Claude Fable plan is written.
- 2026-07-14 · UXF-10 · SOL High QA attempt 2 · PASS · 96 · Repair `075be90` makes reset prefer the current visible recurring row over a hidden duplicate and ignores bubbled keyboard events from nested task controls. Expanded Chunk 8 harness, exact three-error lint baseline, 557-module/18-entry build, diff, clean tree, and cavecrew re-review pass. Existing UXF-10 draft was independently clean at 320/390 signed-out with no overflow or console warnings; it predates the narrow repair, so no second draft was created. Prior signed-in Android evidence remains valid. UXF-10 closed; UXF-9 technical gate planned next.
- 2026-07-14 · UXF-10 · SOL High QA attempt 1 · FAIL · 88 · Lifecycle model, JSONB mapper, UI structure, focused harness, exact lint baseline, 557-module build, clean tree, preview shell, and most recurrence rules pass. Cavecrew found two blockers: hidden-first duplicate recurrence reset can replace the current visible occurrence with stale bytes, and bubbled keyboard events from nested task controls can complete a task while opening Actions. SOL fixer owns repair; Terra not re-invoked.
- 2026-07-14 · UXF-10 · Terra High initial build · CODE_PASS · Feature `3b40a1e`; active-first Checklist, Manage/History, exact completion/Undo lifecycle, recurring source/cycle rules, idempotent saved-list import, compact Dashboard, and JSONB-only archive persistence implemented. Expanded Chunk 8 harness PASS; exact three-error lint baseline; 557-module/18-entry build; diff and cavecrew re-review clean; Android signed-in light/dark Default/Large and Back-close pass; draft `6a56c2018589bea4d591667d` signed-out shell clean. Authenticated draft cloud data unavailable and not claimed. SOL QA next.
- 2026-07-14 · UXF-7 · Owner design approval · PASS · 100 · Maxx approved `docs/MAIN_CHECKLIST_REDESIGN_PROPOSAL.md` as written. UXF-7 closed; UXF-10 implementation unlocked. No source/schema change in proposal work.
- 2026-07-14 · UXF-7 · SOL High proposal · AWAITING OWNER REVIEW · Current one-list source, four item classes, seven resurrection paths, completion-history loss, Dashboard duplication, empty-state mismatch, and mobile friction mapped. Proposal defines recommended UI, exact reset/clear/history/maintenance/template policy, JSONB compatibility, UXF-10 cut, and harness matrix. No application source or schema change.
- 2026-07-14 · UXF-6 · SOL High QA attempt 2 · PASS · 96 · Repair `9d0fb26` makes ISO weekend/service dates local-calendar safe, Days ordinal/DST safe, and normal/quick service defaults local. Expanded harness passes in Eastern, Pacific, and UTC; exact three-error lint baseline, 556-module/18-entry build, full diff, clean tree, final cavecrew review, migration history, schema/default/row/RLS/grant checks, and prior mobile draft shell pass. UXF-6 closed; UXF-7 proposal unlocked.
- 2026-07-14 · UXF-6 · SOL High QA attempt 1 · FAIL · 86 · Main feature, mapper, migration, scope, reset, defaults, harness, lint, build, live schema, and draft shell pass. Review found ISO date-only weekends shift backward west of UTC, Days count elapsed 24-hour blocks instead of calendar days, and Quick Service reopen restores UTC date. SOL fixer owns repair; Terra not re-invoked.
- 2026-07-14 · UXF-6 · Terra High initial build · CODE_PASS · Feature `6cae6cf`; exact Races/Days defaults, local-calendar service boundary, distinct Feature-weekend count, car/rig scope, starting-usage/manual override/service reset, mapper round trip, and 90% behavior pass focused fixtures. Migration `20260714215528` applied once with seven rows retained, default zero, and RLS intact. Exact three-error lint baseline, 556-module/18-entry build, diff, cavecrew, and draft 320/390 signed-out shell pass. Authenticated draft data unavailable; SOL QA next, UXF-7 locked.
- 2026-07-14 · UXF-6 · SOL High planning · READY · Current code counts Feature sessions rather than distinct race weekends and lacks established Feature inference, starting usage, reset, and sync column. Work order fixes those boundaries while preserving manual override, car/rig scope, 90% reconciliation, local-first writes, and delete paths. Live schema/migrations verified read-only; additive column planned, no data rewrite.
- 2026-07-14 · UXF-5 · Owner acceptance · PASS · Maxx approved guide tone on Netlify draft `6a5682d667a1ecb5c117bf39`; UXF-5 closed and UXF-6 unlocked. Draft only; production unchanged.
- 2026-07-14 · UXF-5 · SOL High technical QA attempt 2 · PASS · 96 · Phrase-specific display repair closes all five awkward/academic output findings while preserving byte-equal raw tuning data. Chunk8/9 harnesses, exact three-error lint baseline, 555-module/18-entry build, diff, clean tree, and cavecrew re-review pass. Technical gate complete; owner tone review still required, so UXF-5 remains open.
- 2026-07-14 · UXF-5 · SOL High technical QA attempt 1 · FAIL · 88 · Guide routing, maintenance behavior, raw-data boundary, harnesses, exact lint baseline, build, diff, and runtime auth shell pass. Cavecrew found five remaining awkward/academic rendered phrases missed by corpus guards. Repair transferred to SOL fixer; Terra not re-invoked.
- 2026-07-14 · UXF-4 · SOL High QA attempt 2 · PASS · 96 · Both charts now own larger transparent point targets and explicit focus rings while retaining visible points and keyboard/click/a11y detail. Travel/common-zero/equal-height/PNG/raw-height/data boundaries regress clean. Chunk6a harness, exact three-error lint baseline, 554-module/18-entry build, diff, clean tree, seeded SSR, and cavecrew review pass. UXF-4 closed.
- 2026-07-14 · UXF-4 · SOL High QA attempt 1 · FAIL · 88 · Travel math, common comparison zero, equal-height fallback, upward seeded SVG, raw-height PNG text, unchanged data/CSV/interpolation, chunk6a harness, exact lint baseline, 554-module build, diff, and clean tree pass. Cavecrew found no visible keyboard focus and an 8px comparison touch target. Route to SOL fixer; Terra not re-invoked.
- 2026-07-14 · UXF-3 · SOL High QA attempt 1 · PASS · 97 · Starting/Live-Trackside/Current and evidence-based Raced/Finished labels pass for current and legacy Feature forms. Legacy display mapping preserves local/cloud bytes; lifecycle locks, finish recovery, ownership, merge, and sync remain unchanged. Full chunk6b harness, exact three-error lint baseline, 554-module/18-entry build, diff, clean tree, scope, and cavecrew review pass.
- 2026-07-14 · UXF-2 · SOL High QA attempt 2 · PASS · 96 · Partial Setup/run mismatch now fails before mutation with byte-identical inputs. Full net-coalescing semantics, load/reversal/boundaries, persistence, lock bytes, renderers, chunk7 harness, exact three-error lint baseline, 554-module/18-entry build, diff, scope, clean tree, and cavecrew review pass. UXF-2 closed.
- 2026-07-14 · UXF-2 · SOL High QA attempt 1 · FAIL · 86 · Normal coalescing, latest values, distinct boundaries, reversal, persistence, lock bytes, harness, exact lint baseline, 554-module build, diff, and scope pass. Cavecrew found partial/legacy mismatch: matching Setup row without matching run row reports success and diverges histories. Route to SOL fixer; Terra not re-invoked.
- 2026-07-14 · UXF-1 · SOL High QA attempt 1 · PASS · 97 · Single Maintenance `Other`; pure shared active projection drives ToDo and Dashboard open/mine/count; removed unfinished core stays excluded after JSON round trip. Chunk8 harness, exact three-error lint baseline, 554-module/18-entry build, diff, scope, clean tree, and cavecrew review pass. No schema/package/native/deploy change.
- 2026-07-14 · UXF-8 · SOL High QA attempt 3 · PASS · 96 · Native and GitHub agent roles agree on one Terra pass then first-failure SOL takeover; cold v3/SPRINT_INDEX route, archived paths/content, PROJECT authority, HANDOFF status, migration state, nine-row UXF table, current links, scope, diff check, and clean tree pass. Cavecrew: no issues. Documentation-only; lint/build not required. UXF-8 closed; UXF-1 unlocked.
- 2026-07-14 · UXF-8 · SOL High QA attempt 2 · FAIL · 88 · Four active GitHub roles, archived source path, PROJECT authority, HANDOFF live row, archive preservation, links, migration state, UXF table, scope, diff, and clean tree pass. Cavecrew found one remaining active native-agent mismatch: `.codex/agents/ws-fixer.toml:2` still says takeover after three failed reviews. Route to SOL fixer attempt 3.
- 2026-07-14 · UXF-8 · SOL High QA attempt 1 · FAIL · 82 · Archive moves, current Markdown links, migration state, UXF table, diff check, and no-source-change gate pass. Active `.github/agents` role files still route to retired `plan-v2.md`/`preview-v2`; fixer description still says third failure; active plan source path, PROJECT conflict rule, and live HANDOFF status are stale. First-failure policy routes repair to SOL fixer.
- 2026-07-12 · UX-C1 · Fable QA · PASS-WITH-NOTES · primitives safe (undo state-machine no early-fire/leak; applied pagehide-commit + stepper keyboard). lint 3-baseline, build green, isolated (App.tsx untouched).
- 2026-07-12 · UX-C2 · Fable QA · PASS (no blockers) · back-handling no trap/double-handle; one-shot guards (auto-car/weekend) correct; `byActiveCar` byte-identical; dual-writes intact. Backlog: dashboard 2-press exit; auto-car dup if signed-in pull errors→[]; reportSave(synced) TODO; prune `'quickref'` union.
- 2026-07-12 · UX-C3 · Fable QA · PASS (no blockers) · move-out faithful (weather/image/guards/tire-lifecycle/dual-writes preserved via line-diff); undo delete lazy; diagnostics round-trip; keypad string. Pure fns harness PASS. Backlog: deleting weekend still shows in ContextStrip/Dashboard during undo window.
- 2026-07-12 · UX-C4 · current Codex + cavecrew review · PASS-WITH-NOTES · exact named GPT 5.6 routes unavailable/disclosed. Initial review found 3 critical issues (cross-car service, immediate session delete, fresh-flow continuation); fixed. lint exact 3 baseline; build/Gradle green; Android fresh flow/service-accounting Undo/session Undo pass; draft auth gate clean. Commit `21405e9`; draft `6a5458d75d0c165c44d0ef9f`.
- 2026-07-13 · UX-R1 · SOL/Terra/SOL runtime-verified · PASS · five-tab named Tuning Guide; AFCO class/chassis scoping; light accent + 12px floor; deterministic starter convergence; offline local icons/PWA precache; 320×800 theme × four-font matrix; cold offline Android pass. `turn_context.model` is authoritative; earlier generic self-reports of model unavailability were incorrect.
- 2026-07-13 · UX-C5 · SOL/Terra/SOL + cavecrew · PASS · canonical active-car setup writer; same-car copy/blank/diff; numeric adapters/2×2 corners; always-visible shared four-bar; Tires summary/inventory/history; pressure provenance and blank-import preservation. Both harnesses PASS; lint exact 3 baseline; build 539 modules; Android 4.1 (16) 320 CSS px/XX-Large quick-log and cold-cache persistence pass; final draft `6a5509763fc2865568212af7`. Commit `d5ef1f4`.
- 2026-07-13 · Owner revision · Finish Weekend has no session/race lockout (test days are valid). Setup flow uses immutable Baseline → editable Weekend Setup → immutable Final + editable Current copy. Four-bar requires LR/RR top and bottom rows with frame hole, length, birdcage hole, ride-height angle, full-droop angle. Priority labels get plain-language in-app explanations. Font choices collapse to Default (existing 1.15) and Large (existing 1.45); numeric scale text is hidden.
- 2026-07-13 · UX-C6B · Terra High + cavecrew · CODE_PASS / local RUNTIME_PASS, awaiting SOL · lifecycle harness PASS; exact three-error lint baseline; build 540 modules; strict weekend-owned setup resolution prevents cross-car create/reload; zero-run Finish creates locked Final + editable Current; live migration `20260714020037` verified; draft `6a5599e37111d0563ffaf5f3`.
- 2026-07-13 · UX-C6B · SOL High QA attempt 1 · FAIL · cross-car source leak when starting a second weekend after car switch; legacy weekend Finish lacks `setupId` upgrade path; partial local Finish cannot retry and can duplicate deterministic Final/Current IDs. Schema/RLS/migration, harness baseline, exact lint baseline, build, and draft shell PASS. C7 locked; return to Terra.
- 2026-07-13 · UX-C6B · Terra High repair · CODE_PASS / awaiting SOL QA attempt 2 · same-car new-weekend source; event-owned RaceWeekend setup retained; legacy zero-run Finish upgrade; dangling explicit source rejection; idempotent partial-Finish retry with deterministic dedupe and preserved lock/history timestamps. Expanded harness, exact lint baseline, 540-module build, cavecrew re-review, 390 px draft shell/console PASS. Commit `874fecc`; draft `6a55a024d663b44ca4f639c7`. C7 locked.
- 2026-07-13 · UX-C6B · SOL High QA attempt 2 · FAIL · active weekend with missing owned Setup falls back to selected car in RaceWeekend UI; no-link legacy Finish borrows selected-car data; stale deterministic Final/Current rows are reused while Weekend Setup is unlocked; finished-weekend relationship does not lock an unlocked partial-cloud Weekend snapshot. Direct fixture reproduced Car B borrowing and stale Final overwrite. Harness, exact lint baseline, build, live schema/migration/RLS, and draft HTTP pass; cloud tables have zero lifecycle rows. C7 locked; return to Terra.
- 2026-07-13 · UX-C6B · Terra High repair 2 · CODE_PASS / awaiting SOL QA attempt 3 · event UI resolves owned Weekend Setup or null; no-link legacy Finish uses exact blank values; stale deterministic Final/Current rows rebuild from unlocked Weekend bytes; finished relationship locks Weekend Setup at App/UI boundaries without `lockedAt`; clone remains allowed. Expanded harness, exact three-error lint baseline, 540-module/16-entry build, `git diff --check`, cavecrew review, 390×844 draft shell/console PASS. Commit `7b58b30`; draft `6a55a43a8cc5f6b9da612c69`. No SQL change. C7 locked.
- 2026-07-13 · UX-C6B · SOL High QA attempt 3 · PASS · two cavecrew reviews clean; all four attempt-2 blockers closed; lifecycle harness PASS; exact three-error lint baseline; 540-module/16-entry build; diff check and clean tree PASS; draft 390×844 auth shell/console PASS. Live migration present; 23/23 checked mapped columns, RLS, and authenticated CRUD verified; lifecycle tables empty; zero relevant security-advisor findings; existing performance warnings unchanged. C6B closed; C7 unlocked.
- 2026-07-13 · UX-C7 · Terra High + cavecrew · CODE_PASS / awaiting SOL QA · active unfinished event-owned Setup + real run gate; atomic rapid-safe one-to-one run/Setup logging; spring/J-Bar/shock/load/gear/Other/Four-bar controls; marker lifecycle; pure sync mapper round trip; no migration. Focused harness and diff check PASS; lint exact three-error baseline; cavecrew final review clean. Feature `58e4522`. Full coherent build/cloud/mobile/draft gate deferred until C8 by owner-approved consolidation.
- 2026-07-13 · UX-C7 · SOL High QA attempt 1 · FAIL · off-grid spring/J-Bar steps snap to global grid instead of adding exact delta; stale finished-weekend ID can send selected-car generic Setup into RaceWeekend UI; stale/deleted run can render Quick Adjust because UI gate omits unfinished status and session membership. Harness/diff check pass and lint stays exact baseline, but fixtures missed these edges. Return to Terra; C8 locked.
- 2026-07-13 · UX-C7 · Terra High repair 1 · CODE_PASS / awaiting SOL QA 2 · exact relative numeric deltas preserve off-grid bases; raw stale/invalid/finished weekend selection returns null rather than generic selected-car Setup; run UI requires unfinished event, exact owned Setup, and current session-record membership. Expanded harness, exact lint baseline, diff check, and cavecrew review pass. Repair `030122c`; no SQL. C8 locked.
- 2026-07-13 · UX-C7 · SOL High QA attempt 2 · PASS · all attempt-1 blockers closed; complete Quick Adjust harness PASS; repair-range diff check PASS; exact three-error lint baseline; independent cavecrew review clean. Numeric, ownership, run-membership, atomic history, mapper, marker, load filtering, and lock acceptance pass. No SQL/full runtime gate by consolidation. C7 closed; C8 unlocked.
- 2026-07-13 · UX-C8 · Terra High + cavecrew · CODE_PASS / awaiting SOL QA · visible Maintenance Logs naming; plain interval math; legacy-safe core/adhoc Main Checklist with template re-materialization, 56px row toggle, after-create notes/assignment; stable 90% due-job cycles; recent Accounting defaults/chips. Focused C8 plus C6–C8 harness suite PASS; lint exact baseline; 545-module/16-entry build; diff check and cavecrew re-review clean. Android current-bundle dark mobile naming/automatic injection PASS; draft `6a55b29bbc2a11f748b406a3` 320px auth shell/console PASS. No SQL. C9 locked.
- 2026-07-13 · UX-C8 · SOL High independent QA · PASS · feature/handoff diff inspection and cavecrew review clean; C6A/C6B/C7/C8 harnesses PASS; exact three-error lint baseline; 545-module/16-entry build; diff check PASS. JSONB Todo fields require no migration; current Supabase changes do not affect this shape. Android smoke and 320px draft shell accepted; authenticated task data unavailable and not claimed. C8 closed; C9 unlocked for Terra.
- 2026-07-13 · UX-C9 · Terra High + cavecrew · CODE_PASS / awaiting SOL final QA · pure PDF extraction; actual native/browser file share with quiet cancellation and honest failure; Setup/weekend card actions; direct plain-language help anchors; visible racer-language audit with internal values untouched. C5–C9 plus UX-R1 harnesses PASS; exact three-error lint baseline; 554-module/18-entry build; diff checks and cavecrew final review clean. Android debug native chooser, cancel/back, help, themes/sizes, and offline saved-data boot PASS. Final draft `6a55bd0dfd16f2bd74bf6c1a` passes 320/390 auth shell with no overflow or console errors. No SQL/new package/production/push/master/release change. Feature `c8c4a21`.
- 2026-07-14 · UX-C9 · SOL High QA attempt 1 · FAIL · PDF/share/help and all C5–C9 regression gates pass, but visible hard-coded Shock/J-Bar/Four-bar rows still contain academic phrases (`trailing arm geometry`, `rear roll center`, `chassis roll`, `through apex`, `weight transfer`) because they bypass `plainRacerEffect`. Focused suite PASS; lint exact baseline; 554-module/18-entry build; cavecrew no issues; final draft current-bundle 320/390 clean; Android cold launch/crash clean. Return display-copy-only repair to Terra; no SQL/package/deploy change.
- 2026-07-14 · UX-C9 · Terra High repair 1 · CODE_PASS / awaiting SOL QA 2 · direct guide tables rewritten in accurate dirt-racer language; ordered translator covers remaining academic effect terms without broken grammar; harness decodes, renders, and audits every real effect plus direct copy separately. C9 harness PASS; exact lint baseline; 554-module/18-entry build; diff and final cavecrew review PASS. No PDF/share/lifecycle/sync/schema/package/native/deploy change. Repair `254d928`.
- 2026-07-14 · UX-C9 · SOL High QA attempt 2 · PASS · repair/handoff inspection confirms all 134 real effects are decoded, translated, and audited; direct copy and grammar guards are independent; adjustment direction/cautions preserved. C5–C9 plus UX-R1 harnesses PASS; lint exact three-error baseline; 554-module/18-entry build; repair-range diff and cavecrew review PASS. Prior final draft/Android gate accepted for display-copy-only repair. C9 and Chunks 6B–9 completion run closed; production/push/master/release/schema/packages/native unchanged.

### UX backlog (non-blocking)
- C2: dashboard needs 2 back-presses to exit; wire `reportSave('synced')` into `sync.ts` push helpers; prune dead `'quickref'` in the `activeTab` union. UX-R1 added generation/user guards so stale or failed pulls cannot mark readiness for the wrong auth state.
- C3: pending-delete weekend visible in ContextStrip/Dashboard during 5s undo window (cosmetic); `initialAction='new-session'` with no active weekend opens weekend-create; empty bestLap persists as `"s"` (pre-existing).
- C1: add focus-trap to `BottomSheet` (a11y).
- C4: no-active-car Setups/Tires/Smasher prerequisites were resolved in C5. Global ContextStrip can still show a weekend pending Dashboard undo. App.tsx orchestration grew beyond the ~10-line wiring target; domain-state ownership remains correct.
- C5: unique Netlify draft origin had no remembered login, so authenticated draft feature interaction was not run there; Android preserved-login runtime covered feature paths. `adb install -r` may retain stale Workbox assets; clear only WebView HTTP + Service Worker caches when localStorage must survive.
